/**
 * Unified API Client Export
 * 
 * WHAT: Re-exports the centralized API client
 * WHY: Maintains backward compatibility while using the new unified client
 */
import apiClient from "../services/apiClient";

export default apiClient;
